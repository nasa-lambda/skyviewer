#ifndef ENUMS_H
#define ENUMS_H

// list of possible projections
enum Projection { Spherical, Mollweide };
// list of possible display fields
enum Field { I, Q, U, P, Nobs };
// displaying Polarization vector either on or off
enum PolVectors { Off, On };

#endif
